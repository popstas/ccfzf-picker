# TODO

# next

- [ ] **Проверить открытие сессии в iTerm2, Ghostty и kitty на живом маке.**
  Код написан, форма команды закреплена тестами
  (`test/terminal-presets.test.js`), но ни один из трёх терминалов отсюда не
  запустить — проверять надо на макбуке, где стоят все три (iTerm2 поставлен
  2026-08-14). Пресеты выбираются в настройках, поле «Terminal preset».

  kitty и Ghostty идут прежней дорогой — команда хвостом argv, — и риск у них
  небольшой: kitty так и работал всегда, Ghostty отличается одним `-e`.
  Сомнительный тут iTerm2: в argv он команду не принимает вовсе, и пресет
  ведёт её через `osascript` и `{command}` (см. `COMMAND_PLACEHOLDER` в
  `frontend-src/open-strategy.js`). Кавычек там три слоя — POSIX внутри POSIX
  внутри двойных кавычек AppleScript, — и собрано это вслепую, без единого
  живого запуска. Проверять в первую очередь его, и на каталоге с пробелом в
  имени.

- [ ] **Проверить локальный claude на Windows и macOS — работу без отдельного
  хоста для агентов.** Сейчас список приезжает из `ccfzf --state` по ssh, то
  есть сессии предполагаются на чужой машине. Надо убедиться, что пикер
  работает и когда агенты крутятся там же, где он сам: список без ssh, запуск
  сессии локальным терминалом, отметки «просмотрено», окна и фокус.

- [ ] **На макбуке сессия `obsidian-agent-workspace` не встаёт на место.**
  Должна занимать правую половину экрана, а встаёт не туда. Расстановкой
  занимается трекер той машины (`macos-windows-manager`), а не пикер, — но
  видно это отсюда, при открытии сессии из списка и при восстановлении
  снимка. Выяснить, где теряется место: не подхватывается правило для этого
  имени, не совпадает заголовок окна, с которым идёт привязка, или окно
  расставляется до того, как заголовок устоялся.

- [ ] **kitty должен оставлять окно открытым после завершения команды.** Флаг
  называется `--hold` («Remain open, at a shell prompt, after child process
  exits»), а не `--hide` — такого у kitty нет вовсе, а `--start-as=hidden`
  прячет окно, то есть ровно противоположное. Класть его в оба пресета kitty
  (`terminal-presets.js`, `kitty` и `kitty-linux`) рядом с `--single-instance`.

  Две оговорки. Документация оговаривает, что `--hold` действует «only affects
  the first window», а `--single-instance` отдаёт окно уже запущенному
  экземпляру — проверять надо живьём, на маке (там же, где ждёт проверки
  iTerm2). И `matchPreset` считает выбранный пресет по содержимому полей: у
  того, кто уже сохранил kitty в `config.yaml`, выпадашка после правки покажет
  «Custom», пока он не выберет пресет заново.

- [ ] **Штамп сборки в трее врёт после выкатки: деплой только что, а подпись
  «v0.3.0 · 07:03».** Штамп берётся из `CCFZF_BUILD_UNIX`, который ставит
  `src-tauri/build.rs`, а обновляется он ровно тогда, когда cargo решает
  перезапустить сам build-скрипт. Входы у того объявлены явно —
  `rerun-if-changed` на `src` и `Cargo.toml` (плюс `tauri.conf.json` от
  `tauri_build::build()`), — и фронтенда среди них нет: `frontendDist` —
  `../frontend`, каталог вне пакета. Директиву на него `tauri-build` печатает
  только на пути кодогенерации (`CodegenContext`, `src/codegen/context.rs`), а
  здесь зовётся обычная `tauri_build::build()`, и статику вшивает
  `generate_context!` — proc-макрос, чтения которого cargo не отслеживает.
  Значит, выкатка, принёсшая одни правки страницы, оставляет скрипт свежим, и
  подпись честно показывает прошлую сборку.

  Проверить заодно и второе, более неприятное следствие той же дыры: если
  cargo счёл пакет свежим, он и не перекомпилировал его — то есть новая
  статика, только что разложенная `prepare-frontend.js`, в бинарь не попала
  вовсе, и в трее не врёт ничего, кроме нашего ожидания. Тогда правило из
  `CLAUDE.md` («без `prepare-frontend.js` в бинарь вшилась бы прошлая
  статика») верно лишь наполовину: звать его мало, надо ещё, чтобы cargo
  заметил результат.

  Чинится, вероятно, одной строкой — `rerun-if-changed` на каталог фронтенда в
  `build.rs`; но проверять надо не подпись, а вшитую статику.

- [ ] **Добавить настройки узкой панели: вкладка Panels правит только широкий
  режим.** `panelRows` и `withColumn` (`frontend-src/picker-panels.js`) читают
  и пишут только половинки `.wide`, а `settings.html` рисует таблицу с тремя
  осями, из которых колонка в узком списке смысла не имеет — колонок там нет.

  Сейчас несимметрично: в узком списке порядок задаётся только
  перетаскиванием, свёрнутость — только Enter или кликом по заголовку, а
  спрятать панель нельзя ничем. При этом `normalizeUiState` заводит
  `hidden.narrow` наравне с `hidden.wide`: поле в файле есть, писать в него
  некому. Половинчатое поле хуже отсутствующего — заглянувший в `ui.json`
  решит, что настройка есть, и пойдёт искать, чем её включить.

  **Пикер свою половину работы уже сделал.** `buildSections` отсеивает
  спрятанные секции в обеих раскладках (сторож — «прятанье работает и в узком
  списке» в `test/picker-sections.test.js`), а `sessions.html` передаёт туда
  `hidden[layoutName()]`. Не хватает ровно интерфейса, и задача от этого
  меньше, чем выглядит.

  Две оси вместо трёх: показ и развёрнутость. Колонку в узкую таблицу класть
  нельзя — она там не значит ничего, а показанный и не работающий выбор хуже
  отсутствующего.

  **Главная сложность — набор строк.** В широком режиме ключи постоянны
  (`live`, `remote`, `past`, `projects`, `snapshots`, `zellij`), и таблица из
  шести строк написана заранее. В узком чужие сессии идут по машинам
  (`remote:<host>`), а история умеет делиться по рабочим столам (`past:<N>`) —
  то есть набор панелей зависит от того, что сейчас приехало от агрегатора, а
  окно настроек этого ответа не видит вовсе. Отсюда развилка, которую и надо
  решить: показывать только те ключи, что уже лежат в `ui.json` (их
  `keysInUi` умеет доставать, а `labelForUnknown` — называть), либо научить
  пикер отдавать окну настроек список своих секций. Первое дешевле, но
  спрятать машину можно будет только после того, как её хоть раз свернули или
  перетащили; второе честнее, но заводит между окнами вторую дорогу для
  данных.

- [ ] **Перетаскивание секций и справочник клавиш человеком ещё не проверены.**
  Чек-лист жил в описании PR #5, а PR смержен, и вместе с ним список ушёл из
  виду. Тестами закрыта арифметика (`moveKey`, `dropIndexOf`, порядок секций) и
  вёрстка — измерением в Chromium и WebKit; не закрыт живой путь указателя:
  `setPointerCapture`, придержанный такт, дорисованные пустые колонки,
  гашение клика после переноса. Ровно там уже нашлись две поломки
  (полколонки под свёрнутой секцией и исчезнувшая панель), обе — на живой
  машине, а не в тестах.

  Проверять: перетащить секцию в узком списке и между колонками в широком, в
  том числе в колонку, из которой ушла последняя секция; убедиться, что
  `←/→` ходят туда, куда смотрит глаз; что клик по заголовку по-прежнему
  сворачивает, а не считается переносом; что список не дёргается во время
  переноса. И `?`/F1 — что `^S` виден только при живом трекере.

# future

- [ ] **GitHub-workflow релиза: собирать инсталлятор для Windows и приложение
  для macOS.** Сейчас релиза нет вовсе — обе сборки делаются руками. Нужен
  workflow на тег версии, который гонит `tauri build` на обеих платформах и
  кладёт артефакты в GitHub Release: Windows-инсталлятор (`.msi`/NSIS) и
  macOS-приложение (`.app`/`.dmg`). За образец — workflow релиза в соседнем приложении на том же Tauri 2.

# minor

- [ ] **Порядок панелей пишется по видимому списку, и место отсутствующей
  забывается.** `commitDrag` в `sessions.html` собирает новый порядок из
  колонок, которые сейчас на экране, — значит секция, которой в этот момент
  нет (лёг трекер чужой машины, пусты снимки), из `order` выпадает и,
  вернувшись, встаёт в конец своей колонки. Размен осознанный и записан в
  коде: помнить место того, чего нет, значило бы держать в файле ключи, про
  которые уже не сказать, живые они или мусор. Но выглядит это как забытая
  настройка, и если начнёт мешать — чинится слиянием нового видимого порядка
  со старым сохранённым, а не заменой.

- [ ] **Зелийные строки до списка не доезжают.** `regroup()` в `sessions.html`
  пересобирает ответ агрегатора из одних сессий (`{ ok, sessions, seen, ... }`),
  а `buildSessionsPayload` берёт зелийные сессии из `res.zellij` — которого в
  этом объекте нет. `buildZellijList` получает `undefined`, отдаёт пустой
  список, и группа `Zellij` в живом окне не появляется никогда. Тесты этого не
  ловят: они зовут `buildSessionsPayload` напрямую, с настоящим ответом.
  Найдено при задаче про имя машины чужого окна — там же чинилась соседняя
  потеря верхних полей `windowHost`/`windowPid` в том же пересобранном объекте.
  Чинить, вероятно, там же: либо передавать `zellij`, либо перестать
  пересобирать ответ и класть в `lastState` всё нужное.

- [ ] **Две сессии конкурируют за одно место `recent`.** Сортировать по
  минутам, а не по секундам, иначе на равном возрасте порядок скачет между
  ними от опроса к опросу. Задача приехала из windows-mqtt вместе с пикером.

- [ ] **У залипшей сессии ход бывает старше самой сессии.** На живых данных
  (2026-08-07, 200 сессий) нашлась сессия с `state: "active"`, у которой
  `updated` и `turnAt` — суточной давности, а `started` — пятичасовой. Значит,
  хук перестал писать `state.json`, но `SessionStart` переписал `meta.json`:
  файлы разных возрастов, и в карточке `turn` оказывается старше `started`.
  Причина выше пикера — он показывает то, что приехало, — но выглядит как его
  ошибка: строка утверждает, что сессия «работает» сутки. Лечится на стороне
  хука или отбрасыванием `active` со слишком старым `updated`; ни то, ни другое
  в задачу про время хода не входило.

- [ ] **`resumeCommand` не проверяет `;` в пути, а `newSessionCommand` уже
  проверяет.** Правка про `;` закрыла лазейку только со стороны новой сессии:
  `newSessionCommand` в `frontend-src/open-strategy.js` отказывается собирать
  команду для пути с точкой с запятой, а `resumeCommand` рядом — нет. Для
  Windows Terminal `;` разделитель панелей, и он режет свою командную строку
  до того, как её увидит хоть какой-нибудь шелл, так что `q` тут не спасает:
  сессия в каталоге с `;` в имени развалится на панели вместо запуска. Случай
  тот же, вход другой. Чинить симметрично — либо общей проверкой на обе
  функции, либо отказом в `resumeCommand` с тем же сообщением.

- [ ] **Колонка `hk` заполняется только у строк проектов.** У строк сессий
  `session.hotkey` не ставит никто (`test/row-contract.test.js`, список
  `NO_SOURCE`), то есть галка `hotkeys` в обычном режиме списка включает
  колонку из пустых `div`. Хоткей — признак проекта, а не сессии, так что это
  не поломка, но и не порядок: либо заполнять его у сессии по её `cwd` из того
  же списка проектов (в ответе агрегатора они рядом), либо не показывать
  колонку вне режима проектов.

- [ ] **Процесс без своего транскрипта всё ещё может забрать чужой.** Остаток
  задачи про живость: `fresh_ids()` в агрегаторе отсекает кандидатов по
  возрасту содержимого, но два процесса в одном `cwd` — один разговаривает,
  второй стоит на пустом приглашении — заберут два новейших файла, и второму
  достанется чужая свежая сессия. Честное правило: «файл не может принадлежать
  процессу, если последняя запись в нём старше запуска процесса». Машинерия:
  `starttime` из `/proc/<pid>/stat` уже читается (`proc_started`), но в тиках
  от загрузки — нужен `btime` из `/proc/stat` и `SC_CLK_TCK`, чтобы перевести
  в epoch.

- [ ] **В агрегаторе `age`, сортировка файлов и срез `DUMP_SESSIONS` всё ещё
  считаются от mtime.** Живость от mtime уже отвязали, а эти три — нет, хотя
  ненадёжность mtime доказана там же: две сессии из старой задачи про живость
  по-прежнему показывают возраст в минутах при содержимом недельной давности.
  Хуже того, залежавшийся транскрипт с недавно переписанным хвостом
  выталкивает из среза в 200 строк действительно свежую сессию.

- [ ] **Файл оконного трекера учитывают только `--state` и `--dump`.** Режимы
  `projects` и `sessions` и интерактивный список судят о живости по одному
  `running_sessions()`, поэтому одна и та же сессия читается живой в одном
  представлении и мёртвой в другом.

- [ ] **`last_message_at` в агрегаторе ловит только `ValueError`.** `json.loads`
  до Python 3.6 на `bytes` бросает `TypeError`, а хвост читается именно
  байтами — на такой версии функция упадёт вместо того, чтобы пропустить
  строку. README агрегатора при этом обещает 3.4+. Либо декодировать строку
  перед разбором, либо расширить `except`, либо поднять заявленную версию.
