# TODO

# next

- [ ] **Settings: вкладки.** MQTT — отдельная вкладка: без брокера фичи не
  работают, на Windows и на маке это разные вещи. `Name of this machine`
  перенести в General. Path mapping — своя вкладка с коротким именем;
  Integrations после этого пуста и убирается. Переименовать: Panels → Layout
  panels, UI → Features list (имя выбрать при реализации: это список того, что
  можно показывать в строках), Window → Window size. После разноски уменьшить
  высоту окна настроек, чтобы самая длинная вкладка влезала без вертикального
  скролла.

- [ ] **Settings: таблица Features list.** Шрифтовые иконки у строк, чтобы их
  различать. Пояснения у неоднозначных — как уже у `statusline` и `list`: что
  именно рисуется (`events`, `window`, `window host`). Две группы: Recommended
  и Other; в Other убрать Events и cost (сейчас только они выключены в list,
  остальное — recommended набор). Галка у заголовка Recommended включает все
  Recommended в `list`. Колонки поменять местами: сначала `list`, потом
  `statusline`. Новая строка: опционально шрифтовая иконка приложения терминала
  вместо глифа окна (Windows Terminal, kitty, Ghostty, WezTerm, iTerm).

- [ ] **Settings: хром формы.** Title/tooltip у всех чекбоксов General с
  описанием подробностей. Autosave после правок; больше padding у кнопки Save.
  Подробности терминала, когда пресет не Custom, спрятать в collapsed details.
  Справочник F1: заголовок `ccfzf-picker vX.Y.Z`, «Keyboard shortcuts» —
  подзаголовок; в контенте лицензия MIT и ссылка на GitHub. Выше поле bundle
  ids, чтобы влезали 4 строки. `Projects become stale after` — часы, не дни
  (поле уже есть: `stale.projectDays`).

- [ ] **Выбранный в пикере терминал обязан главенствовать и на машине с
  менеджером — wt и WezTerm на Windows равноправны.** Поймано на popstas-pc:
  выбран пресет `wezterm-windows`, `config.yaml` записан верно
  (`file: wezterm-gui.exe`, `args: [start, --]`), а открывается Windows
  Terminal. Пресет не при чём — на этой машине пикер терминал не открывает
  вовсе: хост совпал с `windowHost`, брокер настроен, и `chooseEnterAction`
  (`frontend-src/open-transport.js`) отдаёт `manager`. Терминал поднимает
  windows11-manager своим конфигом (`launch`/`launchNew` — `wt.exe -w -1 …`),
  а поле `terminal` в `config.yaml` читается только на запасной местной
  дороге, то есть на этой машине почти никогда.

  Переносится не одна команда: `-w -1` («в текущее окно») у `wezterm start`
  прямого аналога не имеет, а `applyWtProfile` вставляет `-p <профиль>` — весь
  маппинг `claudeWt.projects → profile` это понятие Windows Terminal, у
  WezTerm профилей нет вовсе. Дизайн решён и записан:
  [docs/superpowers/specs/2026-08-16-terminal-registry-design.md](superpowers/specs/2026-08-16-terminal-registry-design.md)
  — имя терминала едет в просьбе, определения живут реестром у менеджера,
  профили становятся картой по имени терминала. Работа на три репозитория:
  windows11-manager, ccfzf-picker, macos-windows-manager (последний учится
  открывать сессии и объявляет `openSession: true`).

- [ ] **Вкладка Window: размер выбирается в один клик, и 100% в списке есть.**
  Сейчас там четыре выпадашки (`SIZE_CHOICES` в `frontend-src/settings-form.js`,
  `Default / 50 / 65 / 80 / 95%`), и выбор стоит двух кликов на каждую сторону.
  Надо:
  - **радиокнопки вместо выпадашек** — выбор одним нажатием, все варианты видны
    сразу;
  - **вариант 100%** по обеим сторонам — то самое «во весь экран». Решить при
    этом, `100%` это размер окна в экран или настоящий полноэкранный режим
    (`set_fullscreen` у окна): под окном без декораций разница видна панелью
    задач и вырезом на маке, а `wanted_size`/`scale_axis` в `main.rs` умеют
    только размер;
  - **65% высоты умолчанием** для обеих раскладок. Здесь мина: сейчас ноль (и
    отсутствие ключа) значит «встроенный размер», удалять ключи `merge_patch`
    не умеет, и `Default` — это осознанный пункт списка. Смена умолчания меняет
    поведение у всех, кто `pickerSize` не трогал, — то есть либо `Default` сам
    начинает означать 65%, либо в конфиг проставляется 65 при первом чтении;
    выбрать надо явно, и `narrow_size_matches_the_window_config` про это знает.
  - **свои ширина и высота** — необязательные поля рядом со списком, для тех,
    кому ни один процент не подошёл. Диапазон проверяется в двух местах
    (`scale_axis` в Rust и `normalizePickerSize` на странице), и новая форма
    обязана пройти обе.

- [ ] **Затемнение фона под пикером — по галке на каждую раскладку.** Окно у
  пикера без декораций и стоит поверх всего; в широком режиме оно занимает
  пол-экрана и больше, и граница между списком и тем, что под ним, теряется.
  Затемняющий оверлей эту границу возвращает. Галки **две, раздельные** — узкая
  раскладка и широкая: в узком списке окно маленькое, и затемнение всего экрана
  ради него человек может не захотеть. Обе по умолчанию **выключены** —
  поведение не меняется, пока о нём не попросили.

- [ ] **Широкий режим: набор запроса уводит выбор из той панели, в которой он
  стоял.** Выбрал строку в `Projects` (средняя колонка), начал печатать — и
  выбор оказался в своих живых сессиях, то есть в первой колонке. Причина
  видна в `sessions.html`: правка запроса ставит `selectionReset = true`, а
  сброс уводит выбор на первую строку списка, которая не заголовок
  (`rows.findIndex(r => !HEADER_KINDS.has(r.kind))`) — про панель, в которой
  человек стоял, он не знает вовсе. В узком списке это незаметно (панели идут
  сверху вниз, и первая строка почти всегда та, что нужна), а в широком
  выглядит прыжком через экран. Надо: помнить панель выбранной строки и при
  сбросе вставать на её первую совпавшую строку, а в первую колонку уходить
  только тогда, когда в прежней панели не нашлось ничего. Мина рядом: сброс
  зовётся и на показ окна (`beginShow`), где панели помнить нечего, — умолчание
  «первая не-заголовок» обязано остаться там как есть.

- [ ] **Вместо пути показывать проект — и только когда он не назван в самой
  строке.** Сейчас галка `paths` рисует под именем весь путь через `shortPath`
  (`~/projects/js/ccfzf-picker`), то есть три четверти строки занимает общая у
  всех приставка. Показывать надо серым один basename, и **только если его нет
  в имени строки**: у сессии `mac-wezterm` (id 0dd0) под именем встаёт
  `ccfzf-picker`, а у одноимённой проекту сессии (id a215) не встаёт ничего —
  повтор ничего не сообщает, а строку растит.
  Что решить по дороге:
  - **как сравнивать имя с basename** — подстрокой, по словам или после
    нормализации (`-`/`_`/регистр): имена сессий человек пишет вольно, и
    `ccfzf_picker` рядом с `ccfzf-picker` обязан считаться тем же;
  - **строка проекта** (`projectItem`) — там `label` и есть имя каталога, то
    есть правило погасит подпись у всей панели `Projects` разом. Либо это и
    задумано (путь там повторяет имя), либо строке проекта нужна своя ветка;
    у строки внутри снимка (`snapshot-session`) тот же вопрос;
  - **имя галки**. Сейчас ключ `showPaths` (`sessions.html`, подпись `paths`,
    и второй список подписей — в `settings.html`). Переименование в `projects`
    — правка `ui.json` у всех, кто галку трогал: старый ключ обязан пониматься,
    как это уже сделано для плоской формы галок в `normalizeUiState`. Дешевле
    оставить ключ и сменить одну подпись.
  Полный путь при этом никуда не девается — он в подсказке при наведении
  (`rowTitle` в `frontend-src/session-glyph.js`), и `shortPath` нужен ей
  по-прежнему.

- [ ] **Прятать серый кружок у проектов, где не было ни одной сессии.**

## Нужен дизайн

- [ ] **Комментарий к сессии.** 3–4 слова, отдельной строкой под ответом.
  Синхронизируется в общий список: все хосты видят комментарии каждого.
  Хоткей `Ctrl+M`.

- [ ] **В меню `Ctrl+K` буквы без Ctrl.** Например `o` — Cursor, `m` — comment.

- [ ] **Счётчики `docs/TODO.md` в features list.** По возможности, для проектов,
  в духе skill-do: `☑ 9/20`, задачи верхнего уровня по группам заголовков, для
  первого заголовка — как в `statusline-block.sh`; отдельные галочки в features
  list.

- [ ] **Спеки и планы в списке, как PR.** В ответе пути вида
  `docs/superpowers/specs/…` и `docs/superpowers/plans/…` — иконка в строке
  сессии и пункт «открыть в Cursor» в меню `Ctrl+K`.

- [ ] **Place cascade.** Кнопка в local sessions, когда открыто 2–3 сессии.
  Расставляет окна на основном мониторе на 30 или 50% экрана, в порядке
  сортировки списка.

- [ ] Сейчас первая сессия в списке слишком яркая, когда она не активная. Нужно уменьшить яркость, сделать исключение для первой сессии. Или не менять фон у неактивной сессии при наведении, только прозрачность.

- [ ] Выяснить, откуда берётся дата recent у проекта, сейчас наверх выплывают проекты с открытыми окнами сессий, где давно не было активности. Например, у claude-otel сейчас не было активности 9 часов, а в списке проектов пишет 18 минут.

- [ ] Добавить в статуслайн переключатель затенения stale сессий.

- [ ] Разрешить в fullscreen до 5 колонок.

- [ ] При наведении на строку проекта подсвечивать его сессии во всех остальных
  панелях — local, remote, history, snapshots. Это отдельный режим dim: не тот,
  что затемняет stale, а подсветка по `cwd` наведённого проекта.

# future

- [ ] **Завести местный режим — работу без отдельного хоста для агентов.**
  Задача не про проверку, а про незаведённую ветку, и это выяснено разбором
  2026-08-15: `state_source::fetch` умеет ровно одну дорогу — `ssh <host>
  "ccfzf --state"`, хоста без ssh не бывает вовсе (пустой `sshHost` — ошибка
  конфигурации, сторож `empty_ssh_host_is_a_config_error`). То же и на второй
  половине: команду собирает `buildOpenCommand` всегда через `ssh -t`.
  Значит, нужны две новые половинки — «спрашивать агрегатор у себя» и
  «открывать терминал без ssh», — и сперва их надо спроектировать: где живёт
  признак местной машины, что делать с `q` и `inDir` (шелл теперь свой, а не
  чужой), как ведут себя отметки «просмотрено», окна и фокус. Отдельный вход:
  самого `ccfzf` на маке не стоит, так что до проектирования нужен ещё и он.

  Сам агрегатор с местным claude при этом уже проверен и работает — прогон
  2026-08-15 на Linux: `ccfzf --state` нашёл сессию, запущенную на той же
  машине, с верными `cwd`, `live: true`, `age: 0m`, `turnAt` и `started`.
  Единственная оговорка — `pid: 0` и пустой `tty`, то есть `reptyr` такой
  сессии не предложить; это соседняя задача про привязку процесса к
  транскрипту, а не про местный режим.

- [ ] **GitHub-workflow релиза: собирать инсталлятор для Windows и приложение
  для macOS.** Сейчас релиза нет вовсе — обе сборки делаются руками. Нужен
  workflow на тег версии, который гонит `tauri build` на обеих платформах и
  кладёт артефакты в GitHub Release: Windows-инсталлятор (`.msi`/NSIS) и
  macOS-приложение (`.app`/`.dmg`). За образец — workflow релиза в соседнем приложении на том же Tauri 2.

# minor

- [ ] **Порядок панелей пишется по видимому списку, и место отсутствующей
  забывается.** `commitDrag` в `sessions.html` собирает новый порядок из
  колонок, которые сейчас на экране, — значит секция, которой в этот момент
  нет (лёг трекер чужой машины, пусты снимки), из `order` выпадает и,
  вернувшись, встаёт в конец своей колонки. Размен осознанный и записан в
  коде: помнить место того, чего нет, значило бы держать в файле ключи, про
  которые уже не сказать, живые они или мусор. Но выглядит это как забытая
  настройка, и если начнёт мешать — чинится слиянием нового видимого порядка
  со старым сохранённым, а не заменой.

- [ ] **Зелийные строки до списка не доезжают.** `regroup()` в `sessions.html`
  пересобирает ответ агрегатора из одних сессий (`{ ok, sessions, seen, ... }`),
  а `buildSessionsPayload` берёт зелийные сессии из `res.zellij` — которого в
  этом объекте нет. `buildZellijList` получает `undefined`, отдаёт пустой
  список, и группа `Zellij` в живом окне не появляется никогда. Тесты этого не
  ловят: они зовут `buildSessionsPayload` напрямую, с настоящим ответом.
  Найдено при задаче про имя машины чужого окна — там же чинилась соседняя
  потеря верхних полей `windowHost`/`windowPid` в том же пересобранном объекте.
  Чинить, вероятно, там же: либо передавать `zellij`, либо перестать
  пересобирать ответ и класть в `lastState` всё нужное.

- [ ] **У залипшей сессии ход бывает старше самой сессии.** На живых данных
  (2026-08-07, 200 сессий) нашлась сессия с `state: "active"`, у которой
  `updated` и `turnAt` — суточной давности, а `started` — пятичасовой. Значит,
  хук перестал писать `state.json`, но `SessionStart` переписал `meta.json`:
  файлы разных возрастов, и в карточке `turn` оказывается старше `started`.
  Причина выше пикера — он показывает то, что приехало, — но выглядит как его
  ошибка: строка утверждает, что сессия «работает» сутки. Лечится на стороне
  хука или отбрасыванием `active` со слишком старым `updated`; ни то, ни другое
  в задачу про время хода не входило.

- [ ] **`resumeCommand` не проверяет `;` в пути, а `newSessionCommand` уже
  проверяет.** Правка про `;` закрыла лазейку только со стороны новой сессии:
  `newSessionCommand` в `frontend-src/open-strategy.js` отказывается собирать
  команду для пути с точкой с запятой, а `resumeCommand` рядом — нет. Для
  Windows Terminal `;` разделитель панелей, и он режет свою командную строку
  до того, как её увидит хоть какой-нибудь шелл, так что `q` тут не спасает:
  сессия в каталоге с `;` в имени развалится на панели вместо запуска. Случай
  тот же, вход другой. Чинить симметрично — либо общей проверкой на обе
  функции, либо отказом в `resumeCommand` с тем же сообщением.

- [ ] **Колонка `hk` заполняется только у строк проектов.** У строк сессий
  `session.hotkey` не ставит никто (`test/row-contract.test.js`, список
  `NO_SOURCE`), то есть галка `hotkeys` в обычном режиме списка включает
  колонку из пустых `div`. Хоткей — признак проекта, а не сессии, так что это
  не поломка, но и не порядок: либо заполнять его у сессии по её `cwd` из того
  же списка проектов (в ответе агрегатора они рядом), либо не показывать
  колонку вне режима проектов.

- [ ] **Процесс без своего транскрипта всё ещё может забрать чужой.** Остаток
  задачи про живость: `fresh_ids()` в агрегаторе отсекает кандидатов по
  возрасту содержимого, но два процесса в одном `cwd` — один разговаривает,
  второй стоит на пустом приглашении — заберут два новейших файла, и второму
  достанется чужая свежая сессия. Честное правило: «файл не может принадлежать
  процессу, если последняя запись в нём старше запуска процесса». Машинерия:
  `starttime` из `/proc/<pid>/stat` уже читается (`proc_started`), но в тиках
  от загрузки — нужен `btime` из `/proc/stat` и `SC_CLK_TCK`, чтобы перевести
  в epoch.

- [ ] **Файл оконного трекера учитывают только `--state` и `--dump`.** Режимы
  `projects` и `sessions` и интерактивный список судят о живости по одному
  `running_sessions()`, поэтому одна и та же сессия читается живой в одном
  представлении и мёртвой в другом.

- [ ] **`last_message_at` в агрегаторе ловит только `ValueError`.** `json.loads`
  до Python 3.6 на `bytes` бросает `TypeError`, а хвост читается именно
  байтами — на такой версии функция упадёт вместо того, чтобы пропустить
  строку. README агрегатора при этом обещает 3.4+. Либо декодировать строку
  перед разбором, либо расширить `except`, либо поднять заявленную версию.
