# TODO

# next

- [ ] **Режим «fullscreen»: сессии блоками, а не строками.** Сейчас окно —
  узкий список, где сессия занимает строку, а режимы (`/a` проекты, `/s`
  снимки) переключаются префиксом в строке поиска и показываются по одному
  (`parseQuery` в `frontend-src/picker-mode.js`). В полноэкранном режиме на
  экране помещается больше, и разложить стоит иначе:
  - сессия — блок, а не строка: последний запрос и ответ целиком, насколько
    влезает, а не обрезком в колонке;
  - несколько списков сразу, каждый со своим скроллом. Предложенное деление —
    Local, Remote, History, Snapshots; сверить его с тем, как группирует
    `groupSessions` (`frontend-src/session-groups.js`) сегодня: там `Active
    sessions`, `Zellij` и рабочие столы, то есть деление другое и одно из двух
    придётся выбрать;
  - содержимое `/a` (проекты) — отдельным блоком, а не отдельным режимом;
  - блок последних сессий свёрнутым: только дата и количество.
  Переключается режим по `^F` — хоткей внутри окна, в одном ряду с `^K`, `^A`
  и `^S` (`sessions.html`, ветки `plainCtrl`); `KeyF` там пока свободен, но
  форма «один Ctrl или Cmd плюс буква» занята `isReserved` в
  `frontend-src/action-hotkey.js`, то есть новую встроенную ветку надо
  добавлять туда же, иначе она съест событие у настроенного человеком
  действия. При выполнении — скилл `frontend-design:frontend-design`. Открытые
  вопросы к началу работы: меняется ли при переключении размер окна, что
  делают в режиме Enter и стрелки при нескольких списках, и остаётся ли строка
  поиска с её префиксами.

- [ ] **Свой глобальный хоткей на список проектов (`/a`).** Сейчас глобальный
  хоткей один — `picker_hotkey` в `src-tauri/src/main.rs`, он зовёт
  `toggle_picker`, а в проекты человек попадает уже внутри, набрав `/a` или
  нажав `^A`. Завести второй, открывающий пикер сразу в режиме проектов:
  Windows — `Win+Shift+F10`, mac — `Option+Command+Shift+C`. Умолчание тут не
  одно на обе системы, в отличие от нынешнего `Super+Shift+C`
  (`DEFAULT_HOTKEY_ACCELERATOR`): клавиши разные, значит нужна развилка по
  `target_os` и своя запись для меню трея — расходиться им нельзя, это уже
  сторожит тест у существующего умолчания. Режим живёт в строке поиска, а не
  отдельным флагом (`withProjectPrefix` в `frontend-src/picker-mode.js`),
  поэтому Rust должен не просто показать окно, а сказать странице, с каким
  префиксом открыться. Ключ в `config.yaml` — рядом с `hotkey`, с полем в окне
  настроек (`frontend-src/settings-form.js`) и умолчанием в `config-shape.js`.
  Отказ регистрации не фатален и обязан быть виден — то же правило, что у
  проектных хоткеев.

- [ ] **Убрать серый кружок у строки сессии.** Серый — состояние `idle`
  (`.dot.idle`, `sessions.html`; `dotState` в `frontend-src/session-glyph.js`
  отдаёт его и всему, чего не знает): «закончил, постоял, считается
  проверенным», то есть самое частое состояние в списке — кружок там висит
  почти у каждой строки и ничего не сообщает. Гасить его как у закрытой сессии
  — `background: transparent`, место под кружок остаётся: колонка общая, и
  выброшенный элемент сдвинул бы текст этих строк относительно живых. Не
  трогать серый у строки снимка (`.row.snapshot .dot`) — это другой случай, и
  проверить, что подсказка Shift-наведения (`body.shift .row.markable:hover
  .dot`, оранжевая рамка на месте кружка) на погашенном кружке всё ещё видна.

- [ ] **В меню трея — версия, а у дев-сборки время сборки.** Сейчас в меню три
  пункта (`show`, `settings`, `quit`, `src-tauri/src/main.rs`), и какая
  сборка запущена, узнать неоткуда: `deploy-win.sh` обновляет пикер на месте, и
  после выкатки нечем проверить, что перезапустилось именно новое. Добавить
  неактивный пункт с версией (`env!("CARGO_PKG_VERSION")`), а у дев-сборки —
  дату и время сборки; дату показывать, только когда она не сегодняшняя, время
  — всегда. Время сборки в бинарь надо вшить самому: `build.rs` с
  `cargo:rustc-env`, и с `cargo:rerun-if-changed`, иначе cargo не пересоберёт
  скрипт и штамп застынет на первой сборке. Два вопроса к началу: чем
  отличается «дев» — `debug_assertions` не годится, `deploy-win.sh` собирает
  `cargo build --release`; и что считать «сегодня» — меню строится один раз при
  старте, а пикер живёт в трее сутками, так что у долго не перезапускавшегося
  сравнение с «сегодня» устареет.

# future

- [ ] **GitHub-workflow релиза: собирать инсталлятор для Windows и приложение
  для macOS.** Сейчас релиза нет вовсе — обе сборки делаются руками. Нужен
  workflow на тег версии, который гонит `tauri build` на обеих платформах и
  кладёт артефакты в GitHub Release: Windows-инсталлятор (`.msi`/NSIS) и
  macOS-приложение (`.app`/`.dmg`). За образец — workflow релиза в соседнем приложении на том же Tauri 2.

# minor

- [ ] **Две сессии конкурируют за одно место `recent`.** Сортировать по
  минутам, а не по секундам, иначе на равном возрасте порядок скачет между
  ними от опроса к опросу. Задача приехала из windows-mqtt вместе с пикером.

- [ ] **У залипшей сессии ход бывает старше самой сессии.** На живых данных
  (2026-08-07, 200 сессий) нашлась сессия с `state: "active"`, у которой
  `updated` и `turnAt` — суточной давности, а `started` — пятичасовой. Значит,
  хук перестал писать `state.json`, но `SessionStart` переписал `meta.json`:
  файлы разных возрастов, и в карточке `turn` оказывается старше `started`.
  Причина выше пикера — он показывает то, что приехало, — но выглядит как его
  ошибка: строка утверждает, что сессия «работает» сутки. Лечится на стороне
  хука или отбрасыванием `active` со слишком старым `updated`; ни то, ни другое
  в задачу про время хода не входило.

- [ ] **`resumeCommand` не проверяет `;` в пути, а `newSessionCommand` уже
  проверяет.** Правка про `;` закрыла лазейку только со стороны новой сессии:
  `newSessionCommand` в `frontend-src/open-strategy.js` отказывается собирать
  команду для пути с точкой с запятой, а `resumeCommand` рядом — нет. Для
  Windows Terminal `;` разделитель панелей, и он режет свою командную строку
  до того, как её увидит хоть какой-нибудь шелл, так что `q` тут не спасает:
  сессия в каталоге с `;` в имени развалится на панели вместо запуска. Случай
  тот же, вход другой. Чинить симметрично — либо общей проверкой на обе
  функции, либо отказом в `resumeCommand` с тем же сообщением.

- [ ] **`^A` больше не «выделить всё» в поле поиска.** Отдан под ярлык `/a`.
  Если это окажется неудобно, замена — `^/` или `Alt+A`: обе свободны.

- [ ] **Колонка `hk` заполняется только у строк проектов.** У строк сессий
  `session.hotkey` не ставит никто (`test/row-contract.test.js`, список
  `NO_SOURCE`), то есть галка `hotkeys` в обычном режиме списка включает
  колонку из пустых `div`. Хоткей — признак проекта, а не сессии, так что это
  не поломка, но и не порядок: либо заполнять его у сессии по её `cwd` из того
  же списка проектов (в ответе агрегатора они рядом), либо не показывать
  колонку вне режима проектов.

- [ ] **Процесс без своего транскрипта всё ещё может забрать чужой.** Остаток
  задачи про живость: `fresh_ids()` в агрегаторе отсекает кандидатов по
  возрасту содержимого, но два процесса в одном `cwd` — один разговаривает,
  второй стоит на пустом приглашении — заберут два новейших файла, и второму
  достанется чужая свежая сессия. Честное правило: «файл не может принадлежать
  процессу, если последняя запись в нём старше запуска процесса». Машинерия:
  `starttime` из `/proc/<pid>/stat` уже читается (`proc_started`), но в тиках
  от загрузки — нужен `btime` из `/proc/stat` и `SC_CLK_TCK`, чтобы перевести
  в epoch.

- [ ] **В агрегаторе `age`, сортировка файлов и срез `DUMP_SESSIONS` всё ещё
  считаются от mtime.** Живость от mtime уже отвязали, а эти три — нет, хотя
  ненадёжность mtime доказана там же: две сессии из старой задачи про живость
  по-прежнему показывают возраст в минутах при содержимом недельной давности.
  Хуже того, залежавшийся транскрипт с недавно переписанным хвостом
  выталкивает из среза в 200 строк действительно свежую сессию.

- [ ] **Файл оконного трекера учитывают только `--state` и `--dump`.** Режимы
  `projects` и `sessions` и интерактивный список судят о живости по одному
  `running_sessions()`, поэтому одна и та же сессия читается живой в одном
  представлении и мёртвой в другом.

- [ ] **`last_message_at` в агрегаторе ловит только `ValueError`.** `json.loads`
  до Python 3.6 на `bytes` бросает `TypeError`, а хвост читается именно
  байтами — на такой версии функция упадёт вместо того, чтобы пропустить
  строку. README агрегатора при этом обещает 3.4+. Либо декодировать строку
  перед разбором, либо расширить `except`, либо поднять заявленную версию.
