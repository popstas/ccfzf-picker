# TODO

# next
- [ ] должен быть способ начать новую сессию (ctrl+n), с именем по basename проекта, как в ccfzf
- [ ] должен быть поиск по всем проектам ccfzf, даже если нет claude session, чтобы начать новую сессию или открыть папку, терминал. Например, можно по `/a`, `/all`, хоткею `^A` вызывать поиск по всем проектам
- [ ] заменять `Ctrl+` на `^` в подсказках хоткеев
- [ ] при отсутствии данных context usage считать 0%
- [ ] **Живость сессии судить по последней содержательной записи транскрипта, а
  не по mtime файла.** Правится в `running_sessions()` агрегатора
  (`~/projects/shell/ccfzf/ccfzf`, в снимке — `vendor/ccfzf:847`), ветка
  `fresh`. Замерено 2026-08-08: из семи строк с `live: true` две — мёртвые
  сессии. `f83f46c1` (cup-dashboard) — последнее настоящее сообщение
  `2026-08-05T19:51`, «[Request interrupted by user]»; `0624d3a3`
  (obsidian-agent-workspace) — `2026-08-01T20:11`, неделю назад.
  Механизм: ни у одного живого процесса в argv нет ни `--session-id`, ни
  uuid-`--resume` (`claude`, `claude -n <имя>`, `claude --resume ccfzf-picker`),
  поэтому все семь опознаны третьей веткой — «K процессов в одном cwd забирают K
  новейших `.jsonl` этого каталога, если mtime моложе двух часов». Признак в
  ответе: у всех семи `pid: 0` и `tty: ""` — ветка `fresh` кладёт id в `live`,
  но `procs[sid]` не заполняет, и наружу это видно.
  Отсечка в два часа не спасает, потому что **mtime транскрипта обновляется без
  разговора**: в хвосте обоих файлов лежат служебные записи без `timestamp` —
  `last-prompt`, `agent-name`, `mode`, `pr-link`, — и Claude Code переписывает
  их неделю спустя. Файлы выглядели на 26 и 44 минуты при содержимом трёх- и
  семидневной давности.
  Спасательные проходы мимо по устройству: `reattribute` / `reattribute_by_pid`
  ходят по `procs`, куда эвристические сессии не попадают, а `live_by_hook`
  смотрит отметки хука, которым тут дни против `HOOK_LIVE_TTL = 120` с.
  Чинить двумя частями, и вторая не менее важна первой:
  1. Возраст кандидата брать по последней записи с `timestamp`, а не по
     `os.path.getmtime`. Хвост файла всё равно читается (`tail_facts`), но
     `running_sessions()` идёт раньше и своего чтения не делает — нужен
     точечный разбор хвоста только у кандидатов из cwd безымянных процессов, не
     у всех двухсот.
  2. Не выдавать процессу чужой транскрипт, когда своего у него нет. `0624d3a3`
     достался процессу `claude`, запущенному 2 августа и, судя по всему,
     стоящему на пустом приглашении: транскрипта он не завёл вовсе, а
     эвристика всё равно нашла ему «новейший» — мёртвый — файл.
  Проверять на живом ответе: `ccfzf --state` на pc-virt, сверить `live` с
  выводом `pgrep -a claude` и с временем последнего сообщения в каждом
  транскрипте.
- [ ] **Перестать вендорить `ccfzf`, пути описать в скилле `/claude-wt`.**
  `vendor/ccfzf` — снимок агрегатора с pc-virt (`~/bin/ccfzf` →
  `~/projects/shell/ccfzf/ccfzf`); пикер его не исполняет, он лежит для чтения и
  под `scripts/check-agent-of.py`. Цена — снимок надо не забывать обновлять,
  иначе проверка гоняется по вчерашнему коду; последний коммит про это —
  `dcf1422`. Убрать каталог, а взамен в скилле `/claude-wt`
  (`~/.claude/skills/claude-wt/`) выписать все пути связки: где живёт сам
  агрегатор и его симлинк, где хуки (`~/.claude/hooks/`, состояния в
  `~/.claude/claude-wt/<id>.{state,status,meta}.json`), где транскрипты
  (`~/.claude/projects/<mangled-cwd>/<id>.jsonl`), где дампы
  (`~/.ccfzf.sessions.json`, `~/.ccfzf.projects.json`), где файл оконного
  трекера и конфиг пикера. Заодно решить судьбу `scripts/check-agent-of.py`: без
  снимка ему нужна цель — либо путь параметром, либо переезд в репозиторий
  ccfzf. И поправить `README.md:55` и раздел про снимок, чтобы не обещали
  каталог, которого больше нет.

# future

- [ ] **GitHub-workflow релиза: собирать инсталлятор для Windows и приложение
  для macOS.** Сейчас релиза нет вовсе — обе сборки делаются руками. Нужен
  workflow на тег версии, который гонит `tauri build` на обеих платформах и
  кладёт артефакты в GitHub Release: Windows-инсталлятор (`.msi`/NSIS) и
  macOS-приложение (`.app`/`.dmg`). За образец — workflow релиза в соседнем приложении на том же Tauri 2.

# minor

- [ ] **У залипшей сессии ход бывает старше самой сессии.** На живых данных
  (2026-08-07, 200 сессий) нашлась сессия с `state: "active"`, у которой
  `updated` и `turnAt` — суточной давности, а `started` — пятичасовой. Значит,
  хук перестал писать `state.json`, но `SessionStart` переписал `meta.json`:
  файлы разных возрастов, и в карточке `turn` оказывается старше `started`.
  Причина выше пикера — он показывает то, что приехало, — но выглядит как его
  ошибка: строка утверждает, что сессия «работает» сутки. Лечится на стороне
  хука или отбрасыванием `active` со слишком старым `updated`; ни то, ни другое
  в задачу про время хода не входило.
