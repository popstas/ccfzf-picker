# TODO

# next

- [ ] **`normHost` лежит тремя дословными копиями** — `session-windows.js`,
  `picker-snapshots.js`, `open-transport.js`. Условие рулинга этапа 3
  («третья копия где-либо ещё должна стать поводом вынести общий помощник»)
  выполнено.

- [ ] **`trackerHosts` не проверяет форму элементов `windowHosts`.** Пустой
  объект проходит `.filter(Boolean)`, доезжает до `normHost`/`focusPid` и
  молча даёт нулевой pid, то есть выключает фокус. Файл пишет чужая машина,
  недоверие к его содержимому уместно.

- [ ] **`restoreLocally` не покрыт тестом.** Сторож проверяет только развилку
  `snapshotOwner`; пропуск уже открытых сессий и трактовка пустого
  `sessionIds` как «весь снимок» держатся на одной живой проверке.

- [ ] **Проверить открытие сессии в iTerm2, Ghostty и kitty.** Команда
  собирается одна и та же для любого терминала — `[terminal.file,
  ...terminal.args, 'ssh', '-t', host, remote]` (`buildOpenCommand` в
  `frontend-src/open-strategy.js:170`), — а терминалы принимают команду
  по-разному: kitty исполняет хвост argv как есть (потому сегодняшнее
  умолчание и работает), Ghostty хочет её после `-e`, а iTerm2 в argv не
  принимает вовсе — `open -a iTerm` аргументы до программы не доносит.
  Поэтому проверка, скорее всего, кончится не ответом «работает или нет», а
  правкой: аргументы под каждый терминал (задача ниже) плюс обходной путь для
  тех, кто команду в argv не берёт. На макбуке стоят все три (iTerm2 поставлен
  2026-08-14), проверять там.

- [ ] **Лёгкий выбор терминала в настройках.** Сегодня терминал задаётся двумя
  полями руками: `terminal.file` — текст с путём к бинарю, `terminal.args` —
  построчный список (`frontend-src/settings-form.js:33-34`), а умолчание жёстко
  macOS-ное и жёстко kitty'шное (`/opt/homebrew/bin/kitty` с
  `--single-instance`, `frontend-src/config-shape.js:28`, оно же в
  `test/config-shape.test.js:21`). Человеку, у которого стоит iTerm2, надо
  знать и путь до бинаря, и нужный флаг — а флаг у каждого свой (см. задачу
  выше). Нужен выбор из списка — iTerm2, Ghostty, kitty, Windows Terminal, по
  текущей ОС, — подставляющий и путь, и аргументы, с пунктом «Custom» для
  ручного случая. Поля при этом не убирать: путь у всех разный, и один
  homebrew на arm и на intel даёт два разных префикса.

- [ ] **Менять секции местами перетаскиванием заголовка.** Сегодня порядок
  секций задан кодом: в узком списке это порядок сборки (живые, чужие, история,
  зелий, проекты, снимки), в широком он пересчитывается по колонкам, а номер
  колонки ставит `buildSections` по смыслу секции (`group.remote`,
  `group.past`). Человек порядок не выбирает вовсе, хотя у разных людей сверху
  оказывается разное. Тащить надо за заголовок: строки списка заняты кликом,
  который открывает сессию.

  Половина работы уже сделана заранее: ключи секций стабильны (`live`,
  `remote:<host>`, `past`, `projects`, `snapshots`) и не зависят ни от счёта, ни
  от набора машин — по ним уже помнится свёрнутость. Порядок ложится рядом:
  пятое поле верхнего уровня `ui.json`, своё у каждой раскладки, как
  `collapsed`. Три вещи, за которые придётся заплатить, если не помнить о них
  заранее:

  - **`uiStateToSave` придётся звать пятью аргументами** — и в `sessions.html`,
    и в `settings.html`. Эта мина срабатывала уже дважды: окно настроек, не
    вернувшее поле, гасило сначала запомненный `^F`, потом свёрнутость. Сторож
    на арность есть (`test/settings-page.test.js`), но он сверяет список
    аргументов дословно и его надо продлить.
  - **Сохранённый порядок обязан переживать незнакомые и пропавшие ключи.**
    `remote:<host>` приходят и уходят вместе с машинами: трекер лёг — секции
    нет, приехал новый — ключа нет в файле. Правило то же, что у
    `normalizeCollapsed` (`frontend-src/ui-state.js:71`): что не узнали —
    выбросить, чего нет — поставить по умолчанию, то есть в конец.
  - **`←/→` ходят по тому же порядку, что и глаз** (`moveBetweenBlocks`).
    Разъедься сохранённый порядок с видимым — стрелка уводила бы не туда, куда
    смотришь, и поймать это тестом на одну функцию нельзя: врут они согласованно.

  Перетаскивать можно только там, где секции — постоянный список, то есть при
  пустом запросе и без префикса. Это ровно признак `foldable`
  (`frontend-src/picker-sections.js:200`): под запросом набор секций временный,
  заголовок там и так не строка, а подпись, и запоминать порядок временного
  набора значило бы записать в файл случайное.

  Две ловушки в реализации, обе стоят дороже, чем выглядят:

  - **Список перерисовывается раз в секунду**, и `planListSync` правит те
    элементы, что изменились. Перетаскиваемый узел подменится под указателем
    прямо в движении. Такт на время перетаскивания придётся придержать; в
    широкой раскладке вдобавок меняется `blocksShape` (`sessions.html:1042`),
    а это пересборка контейнеров целиком — она обязана случиться после
    отпускания, а не во время.
  - **HTML5 drag-and-drop в этом окне на Windows не работает.** У вебвью Tauri
    `dragDropEnabled` по умолчанию `true`, и системный обработчик файлового
    перетаскивания съедает события страницы; в `tauri-utils` это записано прямо
    в doc-комментарии поля: «Disabling it is required to use HTML5 drag and drop
    on the frontend on Windows». В `tauri.conf.json` ключ не выставлен вовсе, то
    есть действует умолчание. Выхода два: выключить его в конфиге окна (файлы в
    пикер и так не бросают) либо делать перетаскивание на pointer-событиях
    (`pointerdown`/`pointermove`/`setPointerCapture`), которых это не касается.
    Второй путь предпочтительнее и по первой ловушке: своё перетаскивание проще
    согласовать с придержанным тактом, чем чужое.

  Открытый вопрос на решение при реализации: пускать ли секцию из колонки в
  колонку в широком режиме. Сейчас колонку задаёт смысл секции, и это записано
  правилом в `CLAUDE.md`; ручное перемещение между колонками потребует слоя
  поверх этого правила, а перестановка внутри колонки — нет.

- [ ] **В статуслайне оставить `^K` и кнопку `?`, а список клавиш убрать в
  модальное окно.** Подсказка живёт одной строкой в `paintToggles`
  (`sessions.html:732`) и растёт с каждой новой клавишей — сегодня в неё
  добавились `^P`, `^L` и `^H`, и она заняла три строки кода и всю ширину
  статуслайна. При этом она же и недоговаривает: встроенных действий
  (`^N`, `^G`, `^U`, `^D`, `^R`, `^I`) в ней нет вовсе, настроенных человеком —
  тоже, а глобальных хоткеев, работающих при скрытом окне, — тем более. То есть
  строка одновременно переполнена и неполна.

  **Образец брать с `#info-backdrop`, а не с `#actions-menu`.** Модалок в окне
  уже две, и та, что открывается по `^K`, для этого не годится: она про
  действия над строкой — ей нужен `rows[active]`, её пункты что-то запускают, у
  неё есть выбор стрелками и `menuActive`. Справочник клавиш — статичный список
  «клавиша → что делает», а это ровно то, что уже рисует `#info-card`:
  `.info-row` с `.k` и `.v` (`sessions.html:411`). Слова «как по `^K`» в
  постановке — про вид окна, не про его устройство.

  **Главная работа — не окно, а единственный источник списка.** Сейчас клавиши
  разложены по четырём местам, и таблицей не является ни одно:

  - встроенные действия — `BUILTIN_ACTION_KEYS` (`frontend-src/action-hotkey.js`);
  - ярлыки режимов `^K`, `^P`, `^L`, `^H`, `^S`, `^F`, `^,` — написаны буквально
    в ветках обработчика клавиш и **второй раз** строками в `RESERVED_CODES`;
  - настроенные человеком действия — `CONFIG.actions[].hotkey`;
  - глобальные (работают при скрытом окне) — `hotkey` и `projectsHotkey` из
    конфига, их регистрирует Rust, плюс проектные хоткеи из ответа агрегатора.

  Написать список руками третьей копией нельзя: за эту ошибку в проекте уже
  заплачено — правило про `FILTER_KEYS`, считанный из `TOGGLE_CHECKS`, стоит в
  `CLAUDE.md` именно потому, что второй список молча разошёлся бы с первым.
  Здесь разойтись он может тише некуда: окно показывало бы клавишу, которую
  никто не слушает. Значит, справочник и `RESERVED_CODES` обязаны расти из
  одной таблицы — либо у записей `RESERVED_CODES` появляется подпись, либо обе
  собираются из общего списка.

  **Условность подсказок обязана дожить до окна.** `^S` показывается только при
  `trackerIsHere()`, и причина записана прямо над строкой: подсказка про режим,
  которого нет, обманывает так же, как молчащий Enter. Отдельный вопрос на
  решение — что делать с проектным хоткеем, который не встал (`hotkeysTaken`):
  показать зачёркнутым или не показывать вовсе. Сейчас про него говорит только
  подпись в `#message`.

  Мелочи, которые всё равно всплывут:

  - Кнопка `?` встаёт **перед** шестерёнкой: `#settings-button` прижат вправо
    своим `margin-left: auto`, и поставленная после неё кнопка отобрала бы у
    шестерёнки место последней. `title` — по-английски, как у соседки.
  - Третий оверлей — третья ветка в начале обработчика клавиш: тот сейчас
    разбирает `infoOpen` и `menuOpen` подряд (`sessions.html:2137`). Обобщать
    или дописать третью — решать при реализации, две ветки ещё не образец.
  - **Если вешать на это клавишу, то `F1`, а не `?`.** Поле поиска
    сфокусировано всегда, `?` — печатный знак, и прямая ветка отобрала бы его у
    поиска насовсем: та же цена, за которую `seen` уехал с `^V` на `^D`.
    С `RESERVED_CODES` ни `F1`, ни `?` не спорят — там заняты только формы «один
    Ctrl или Cmd плюс буква». Но по постановке хватает и одной кнопки мышью.

# future

- [ ] **GitHub-workflow релиза: собирать инсталлятор для Windows и приложение
  для macOS.** Сейчас релиза нет вовсе — обе сборки делаются руками. Нужен
  workflow на тег версии, который гонит `tauri build` на обеих платформах и
  кладёт артефакты в GitHub Release: Windows-инсталлятор (`.msi`/NSIS) и
  macOS-приложение (`.app`/`.dmg`). За образец — workflow релиза в соседнем приложении на том же Tauri 2.

# minor

- [ ] **Зелийные строки до списка не доезжают.** `regroup()` в `sessions.html`
  пересобирает ответ агрегатора из одних сессий (`{ ok, sessions, seen, ... }`),
  а `buildSessionsPayload` берёт зелийные сессии из `res.zellij` — которого в
  этом объекте нет. `buildZellijList` получает `undefined`, отдаёт пустой
  список, и группа `Zellij` в живом окне не появляется никогда. Тесты этого не
  ловят: они зовут `buildSessionsPayload` напрямую, с настоящим ответом.
  Найдено при задаче про имя машины чужого окна — там же чинилась соседняя
  потеря верхних полей `windowHost`/`windowPid` в том же пересобранном объекте.
  Чинить, вероятно, там же: либо передавать `zellij`, либо перестать
  пересобирать ответ и класть в `lastState` всё нужное.

- [ ] **Две сессии конкурируют за одно место `recent`.** Сортировать по
  минутам, а не по секундам, иначе на равном возрасте порядок скачет между
  ними от опроса к опросу. Задача приехала из windows-mqtt вместе с пикером.

- [ ] **У залипшей сессии ход бывает старше самой сессии.** На живых данных
  (2026-08-07, 200 сессий) нашлась сессия с `state: "active"`, у которой
  `updated` и `turnAt` — суточной давности, а `started` — пятичасовой. Значит,
  хук перестал писать `state.json`, но `SessionStart` переписал `meta.json`:
  файлы разных возрастов, и в карточке `turn` оказывается старше `started`.
  Причина выше пикера — он показывает то, что приехало, — но выглядит как его
  ошибка: строка утверждает, что сессия «работает» сутки. Лечится на стороне
  хука или отбрасыванием `active` со слишком старым `updated`; ни то, ни другое
  в задачу про время хода не входило.

- [ ] **`resumeCommand` не проверяет `;` в пути, а `newSessionCommand` уже
  проверяет.** Правка про `;` закрыла лазейку только со стороны новой сессии:
  `newSessionCommand` в `frontend-src/open-strategy.js` отказывается собирать
  команду для пути с точкой с запятой, а `resumeCommand` рядом — нет. Для
  Windows Terminal `;` разделитель панелей, и он режет свою командную строку
  до того, как её увидит хоть какой-нибудь шелл, так что `q` тут не спасает:
  сессия в каталоге с `;` в имени развалится на панели вместо запуска. Случай
  тот же, вход другой. Чинить симметрично — либо общей проверкой на обе
  функции, либо отказом в `resumeCommand` с тем же сообщением.

- [ ] **Колонка `hk` заполняется только у строк проектов.** У строк сессий
  `session.hotkey` не ставит никто (`test/row-contract.test.js`, список
  `NO_SOURCE`), то есть галка `hotkeys` в обычном режиме списка включает
  колонку из пустых `div`. Хоткей — признак проекта, а не сессии, так что это
  не поломка, но и не порядок: либо заполнять его у сессии по её `cwd` из того
  же списка проектов (в ответе агрегатора они рядом), либо не показывать
  колонку вне режима проектов.

- [ ] **Процесс без своего транскрипта всё ещё может забрать чужой.** Остаток
  задачи про живость: `fresh_ids()` в агрегаторе отсекает кандидатов по
  возрасту содержимого, но два процесса в одном `cwd` — один разговаривает,
  второй стоит на пустом приглашении — заберут два новейших файла, и второму
  достанется чужая свежая сессия. Честное правило: «файл не может принадлежать
  процессу, если последняя запись в нём старше запуска процесса». Машинерия:
  `starttime` из `/proc/<pid>/stat` уже читается (`proc_started`), но в тиках
  от загрузки — нужен `btime` из `/proc/stat` и `SC_CLK_TCK`, чтобы перевести
  в epoch.

- [ ] **В агрегаторе `age`, сортировка файлов и срез `DUMP_SESSIONS` всё ещё
  считаются от mtime.** Живость от mtime уже отвязали, а эти три — нет, хотя
  ненадёжность mtime доказана там же: две сессии из старой задачи про живость
  по-прежнему показывают возраст в минутах при содержимом недельной давности.
  Хуже того, залежавшийся транскрипт с недавно переписанным хвостом
  выталкивает из среза в 200 строк действительно свежую сессию.

- [ ] **Файл оконного трекера учитывают только `--state` и `--dump`.** Режимы
  `projects` и `sessions` и интерактивный список судят о живости по одному
  `running_sessions()`, поэтому одна и та же сессия читается живой в одном
  представлении и мёртвой в другом.

- [ ] **`last_message_at` в агрегаторе ловит только `ValueError`.** `json.loads`
  до Python 3.6 на `bytes` бросает `TypeError`, а хвост читается именно
  байтами — на такой версии функция упадёт вместо того, чтобы пропустить
  строку. README агрегатора при этом обещает 3.4+. Либо декодировать строку
  перед разбором, либо расширить `except`, либо поднять заявленную версию.
