# TODO

Список того, что осознанно отложено. Задачи 1–14 плана закрыты.

Подробные план и спек — внутренние документы, оставшиеся вне репозитория.
Ссылок на них здесь нет: каждый пункт ниже описан так, чтобы его можно было
понять и взять в работу по одному этому файлу.

## Решено оставить как есть

- [ ] **`takeover`: `kill -HUP` без проверки успеха.**
  `frontend-src/open-strategy.js`, ветка `takeover`. Четыре команды склеены
  через `;`, а не `&&`, и `kill -0` возвращает ненулевой код и на ESRCH
  («процесса нет»), и на EPERM («процесс жив, не дотянуться»). Если `kill -HUP`
  не сработал вовсе, все три проверки читают это как «процесс ушёл», и
  `claude --resume` запускается — ровно тот случай с двумя процессами на одном
  транскрипте, который комментарий в этом же коде обещает не допустить.

  Решение владельца проекта (2026-08-05): **оставить как в плане.** Практически
  EPERM почти невозможен — процесс того же uid. С `caps.reptyr: true` ветка
  вдобавок стала недостижимой: перехват выбирается, только если reptyr выключен
  в конфиге.

  Если когда-нибудь захочется закрыть: менять `;` на `&&` **нельзя** — тогда
  сессия, умершая между обновлением списка и кликом, даст ESRCH, цепочка
  оборвётся, и человек не получит ничего. Нужно различать два отказа:

  ```sh
  kill -HUP 42 || kill -0 42 2>/dev/null && { echo "…cannot signal 42" >&2; exit 1; }
  ```

  то есть отказываться только когда сигнал не прошёл **и** процесс всё ещё
  виден. Вторая дверь в ту же дыру: `Number(row.pid)` на нечисловом pid даёт
  литерал `NaN`, и все проверки снова читают «процесса нет». Закрывается
  проверкой `Number.isInteger(pid)` перед сборкой ветки.

## Найдено при выполнении задач 9–14

- [ ] **Два процесса на одном транскрипте теперь допускаются сознательно.**
  Живая сессия открывается через `resume`, то есть рядом с уже работающей
  копией. Решение владельца проекта (2026-08-05): открывать повторно лучше,
  чем убивать чужую работу, раз переносить нечем. Перехват остался за
  `caps.takeover`, по умолчанию выключенным.

  Чем это может аукнуться, никто не проверял: две копии claude пишут в один
  файл транскрипта. Если всплывёт порча истории — вот первое место, куда
  смотреть.

- [ ] **`confirm()` в ветке перехвата не проверен.**
  Диалог подтверждения рисует `window.confirm` в webview. С `caps.reptyr: false`
  эта ветка теперь основная для живых сессий, так что проверить надо в первую
  очередь: если WKWebView вернёт `false` молча, перехват просто не сработает, и
  без единого сообщения.

- [ ] **Список на маке и на Windows совпасть не могут.**
  Пикер на Windows берёт сессии из `state.slots` оконного трекера
  (оконный трекер соседа): там те, за кем закреплено окно
  терминала. На маке трекера нет, спек его сознательно выкинул.

  Решено (2026-08-05) показывать только `live` — `onlyLive: true` в конфиге.
  Это ближайшее по смыслу, но не то же самое: сессия с открытым окном на
  Windows может быть уже не `live` по мнению ccfzf (так было с `18ea`), а живых
  по ccfzf сейчас семь против пяти окон. Полного совпадения не будет без
  своего учёта открытых терминалов на удалённом хосте — а это возвращает в проект ровно
  то отслеживание окон, от которого уходили.

  Побочное: с `onlyLive` поиск ищет по семи строкам, а не по двум сотням.
  Старую сессию из пикера теперь не найти — только `onlyLive: false`.

- [x] **Повторный перенос той же сессии падал.** Разобрано 2026-08-05 (опыты
  3–6, см. архив скилла claude-wt). Первая кража выносит мастер pty из sshd, а
  второй `-T` приходит на то же место и не находит ничего:
  `Unable to find the fd for the pty!`. Лечится тем, что вторым и последующим
  разом переносится не сессия, а сам reptyr — плоской формой. Команда в
  `buildAttachCommand` теперь умеет оба случая.

  `caps.reptyr` при этом **остаётся `false`**: команда проверена на простой
  жертве, а на живом claude первый перенос владелец делает руками. Включать
  автоматический перенос — после того, как ^R поработает на живых сессиях.

- [ ] **Перенос держится на capability, выданных вручную.**
  `reptyr -T` работает только потому, что на `/usr/bin/reptyr` выданы
  `cap_sys_ptrace,cap_dac_read_search+ep` (см. архив скилла claude-wt).
  Обновление пакета `reptyr` их снимет — и пункт ^R начнёт отдавать команду,
  которая молча не работает. Проверять при обновлениях: `getcap /usr/bin/reptyr`.

- [ ] **tmux отпадает: перехватывает хоткеи, скролл и OSC.**
  Ветка `attach` сняла бы разом и перенос, и две копии на транскрипте, но
  владелец от мультиплексоров отказался — тот же опыт был с zellij. Записано,
  чтобы не предлагать это снова.

- [x] **Сортировка и чекбоксы statusline не переживают перезапуск.** Закрыто
  2026-08-05 (`ui.json` рядом с `seen.json`, коммит «вид списка переживает
  перезапуск»). Отдельный файл, а не поля в `config.yaml`: конфиг пишет
  человек, а это пикер переписывает на каждый клик — он затирал бы
  комментарии.

- [x] **Агент поднимался мимо `.zshrc`.** Найдено и закрыто 2026-08-05.
  `ssh host cmd` — неинтерактивный шелл, zsh читает тогда только `.zshenv`, и
  хук `chpwd` не ставит `project=` в `OTEL_RESOURCE_ATTRIBUTES`: вся телеметрия
  шла без имени проекта. Лечится формой `inDir` (`exec $SHELL -ic 'cd -- <cwd>
  && <cmd>'`), той же, что у ccfzf. Через неё теперь идут все три места:
  `resume`, хвост `takeover` и проектные хоткеи.

## Сборка и релиз

- [ ] **GitHub-workflow релиза: собирать инсталлятор для Windows и приложение
  для macOS.** Сейчас релиза нет вовсе — обе сборки делаются руками. Нужен
  workflow на тег версии, который гонит `tauri build` на обеих платформах и
  кладёт артефакты в GitHub Release: Windows-инсталлятор (`.msi`/NSIS) и
  macOS-приложение (`.app`/`.dmg`). За образец — workflow релиза в соседнем приложении на том же Tauri 2.

## Мелочи

- [ ] **`--dump` в худшем случае стал дороже.** `running_sessions()` теперь
  зовёт `proc_tmux` на каждую живую сессию, а тот запускает `tmux display` с
  таймаутом 2 с. Замеры — 0.3 с на всё, но зависший tmux-сервер может
  добавить до 2 с на каждую живую сессию внутри tmux. `dump` делит эту функцию
  с `state`, а пикер на Windows ждёт именно `dump`.

- [ ] **Перенос reptyr проверен только с `tmux-server` в роли держателя pty.**
  В бою держателем будет `sshd: <user>@pts/N` — он от того же юзера, не root,
  так что `ptrace_scope=0` хватит. Эмпирически с sshd и kitty так и не
  проверялось: Task 12 должна была это закрыть, но проверка требует рук.

- [x] **`session-groups.js` кладёт `monitor: null` в каждую группу.** Поле было
  мёртвое — досталось от соседнего пикера, где группы делятся по мониторам.
  Убрано 2026-08-05.
